import React from 'react';
import Wrapper from '../../../pages/components/sub-wrapper';
import { data } from './data';

const Index = () => {
  return <Wrapper data={data} />;
};

export default Index;
